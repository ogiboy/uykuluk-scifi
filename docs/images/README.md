# Screenshot Inventory

The v0.82 production-build browser UAT captured these stable, repository-relative files:

- `docs/images/studio-dashboard.png`: Studio home with current run and next safe action.
- `docs/images/studio-run-detail.png`: run detail with workflow progress and review rail.
- `docs/images/studio-voice-audition.png`: candidate cards, local A/B preview, and selection
  controls.
- `docs/images/studio-voice-evidence.png`: quote, approval, quota, synthesis, and alignment summary.

No placeholder binaries are committed. The captures use synthetic local fixtures and contain no API
keys, local absolute paths, provider request payloads, or private episode content. Refresh them only
from a successful production build and browser UAT when the documented surface materially changes.
