[project]
name = "uykulukscifi-mflux-runtime"
version = "0.1.0"
description = "Pinned optional local image-generation runtime for UykulukSciFi Studio"
requires-python = ">=3.12,<3.13"
dependencies = ["huggingface-hub>=1.24.0", "mflux==0.18.0"]

[tool.uv]
package = false
